import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "JTM Hostel Management System",
  description:
    "Hostel Management System (HMS) for Jabatan Tenaga Manusia (JTM), Malaysia — manage blocks, rooms, students, and furniture inventory with a modern glassmorphism interface.",
  keywords: ["JTM", "Hostel Management", "Asrama", "Jabatan Tenaga Manusia", "Malaysia"],
  authors: [{ name: "JTM Software Engineering Team" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
